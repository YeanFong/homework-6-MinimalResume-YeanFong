<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>{{pageTitle}}</title>
    {{{_sections.styles}}}
  </head>
  <body>
    {{{body}}}
    {{{_sections.sidebar}}}
  </body>
</html>